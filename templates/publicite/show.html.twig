{% extends 'base.html.twig' %}

{% block title %}Publicite{% endblock %}

{% block body %}
    <h1>Publicite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publicite.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ publicite.image }}</td>
            </tr>
            <tr>
                <th>Produit</th>
                <td>{{ publicite.produit }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ publicite.duree ? publicite.duree|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_publicite_index') }}">back to list</a>

    <a href="{{ path('app_publicite_edit', {'id': publicite.id}) }}">edit</a>

    {{ include('publicite/_delete_form.html.twig') }}
{% endblock %}
