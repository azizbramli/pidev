<form method="post" action="{{ path('app_publicite_backoffice_delete', {'id': publicite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publicite.id) }}">
    <button class="ri-delete-bin-line"></button>
</form>
